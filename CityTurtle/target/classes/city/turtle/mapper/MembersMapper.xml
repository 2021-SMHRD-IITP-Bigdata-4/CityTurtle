<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="city.turtle.mapper.MembersMapper">
    <insert id="insertMembers" parameterType="city.turtle.mapper.MembersVO">
        insert into members(mb_id, mb_nick, mb_pwd, mb_name, mb_birthdate, mb_sex, mb_email)
        values(#{mb_id},#{mb_nick},#{mb_pwd},#{mb_name},#{mb_birthdate},#{mb_sex},#{mb_email})
    </insert>
    
</mapper>